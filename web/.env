# PostHog. The project token is a write-only public key that ships inside the
# client bundle anyway, so it is committed here to keep every build — any
# machine, any CI runner — actually sending events. Without it the site builds
# fine and silently reports nothing.
NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN=phc_oJBJxjc2hSbqnYpoTQV8wYey7HLYzpTA8p9YHvAuytC3
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
